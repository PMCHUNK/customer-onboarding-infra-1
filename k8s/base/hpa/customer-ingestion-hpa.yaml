apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: customer-ingestion-hpa
  labels:
    app: customer-ingestion
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: customer-ingestion
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50
